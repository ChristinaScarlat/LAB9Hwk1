public class Main4 {
    public static void main(String[] args) {
        //Given a number, while the number
        //is equal to or lower than 100, print the number;
        //1. Creati o metoda in clasa LogicalOp, care sa primeasca
        // un numar ca si parametru. Metoda sa verifice numarul primit,
        // si sa afiseze in consola o numaratoare pana la 100,
        // pornind de la numarul primit ca si parametru.
        // Atentie la crearea tipului metodei, deoarece metoda afiseaza,
        // nu returneaza.
        //
        // Apelati metoda in clasa Main, metoda main() pentru a verifica daca functioneaza.
      LogicalOperations op = new LogicalOperations();
     //  int x = 12;
      //  op.printToHundred(x);

        //Write a program
        // called SumAndAverage to produce the
        // sum of 1, 2, 3, ..., to 100. Also
        // compute and display the average.
        //8. Creati o metoda in clasa LogicalOp care sa primeasca
        // un parametru de tip numar. Metoda sa adune toate numerele
        // pana la 100, pornind de la numarul primit ca si parametru,
        // si sa calculeze media numerelor. La final,
        // metoda sa returneze media.

     // System.out.println(op.sumAndAverage());

//Modify the program to sum from 111 to 8899, and compute the average.
// Introduce an int variable called count to count the
// numbers in the specified range.

      // System.out.println(op.getAverageInInterval(111,8899) );

//4. Write a program to sum only
// the odd(even) numbers from 1 to 100, and compute the average.
      // System.out.println(op.getAverageForEvenInInterval(1,100));
     //  System.out.println(op.getAverageForOddInInterval(1,100));

 //2. Creati o metoda in clasa LogicalOp, care sa primeasca un numar
 // ca si parametru. Metoda sa verifice numrul primit, si sa
 // afiseze in consola o numaratoare pana la -100, pornind de la
// numarul primit ca si parametru. Atentie la crearea tipului
// metodei, deoarece metoda afiseaza, nu returneaza.

// Apelati metoda in clasa Main, metoda main() pentru a verifica
// daca functioneaza.

      //  int z = -22;
       // op.printToHundredminus(z);
//3. Creati o metoda in clasa LogicalOp, care sa primeasca doi
// parametrii de tip numar. Metoda sa afiseze o numaratoare intre cele
// doua numere primite, pornind de la primul parametru primit, pana la
// cel de-al doilea(ex: daca int x e primul parametru iar int y  este
// cel de-al doilea, numaratoarea sa se faca de la x, la y.
//Atentie la crearea tipului metodei, deoarece metoda afiseaza,
// nu returneaza.

        //int a = 10;
       // int b = 107;
        //op.printaTob(a,b);
//4. Creati o metoda in clasa LogicalOp, care sa primeasca doi parametrii
// de tip numar. Metoda trebuie sa verifice care dintre cele doua numere
// este mai mare, si sa execute o numaratoare crescatoare de la cel mai
// mic la cel mai mare.(ex: daca int x e primul parametru iar int y  este
// cel de-al doilea, daca x  este mai mare decat y,  atunci numaratoarea
// sa fie de la y la x).
//Atentie la crearea tipului metodei,
// deoarece metoda afiseaza, nu returneaza.

     //   double X = 1000;
    //    double Y = 1070;
    //    op.printYToX(Y,X);

//5. Creati o metoda in clasa LogicalOp care sa afiseze toate numerele
// pare de la 1 la 100.

       // int r = 0;
      //  op.printEvenNumber(r);

//6. Creati o metoda in clasa LogicalOp care sa afiseze toate numerele
// impare de la 1 la 100.
     //   int s =1;
     //   op.printOddNumber(s);

//7. Creati o metoda in clasa LogicalOp care sa primeasca un parametru
// de tip numar. Metoda sa adune toate numerele pana la 100, pornind
// de la numarul primit ca si parametru, iar la final sa returneze
// rezultatul. Apelati metoda in clasa Main, metoda main() pentru
// a verifica daca functioneaza.
         System.out.println(op.sum());





    }
}
